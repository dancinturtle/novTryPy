# The method index() returns the lowest index in list that obj appears.
# This difference better find and index is that when using if the value
# is not found then it will return an error. In case of find it will return -1

aList = [123, 'xyz', 'zara', 'abc'];

print aList.index('123')
print aList.index('xyz')
print aList.index('zara')
